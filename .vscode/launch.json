{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch OCRGO",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "buildFlags": "-ldflags '-linkmode internal'"
        }
    ]
}